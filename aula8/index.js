// Diferença entre Var e Let

varGlob = 'angelo'   // NÃO FAZER ISSO

/*(var nome = 'Angelo'
var nome = 'Ridolfi'   // Variavel declarada com VAR pode ser re-declarada com outro valor, com o let isso não acontece */

let nome = 'Angelo' // Pode receber outro valor, porém nao pode redeclarar a variavel.

console.log(nome)